@{
    ViewBag.Title = "DSTaiLieu";
    Layout = "~/Views/Share/_LayoutFirst.cshtml";
}
@section Slider{
    <div id="SliderGioiThieu">
    </div>
}
<h2 id="Tua">Danh sách tài liệu</h2>
@using WebDauTienVS2._0.Models;
@using PagedList.Mvc;
@model PagedList.IPagedList<WebDauTienVS2._0.Models.PageItem>  
<div id="Content">
        <button id="btXuLyTrai">
            <i  class="fas fa-search"></i> @Html.ActionLink("Tra cứu tài liệu", "timKiem", "TimKiem")
        </button>
        <button id="btXuLyPhai">
             <span><i  class="fas fa-hand-holding"></i> @Html.ActionLink("Đóng góp tài liệu", "DongGopTaiLieu", "Home") </span>
        </button><br /><br />
    @using (Html.BeginForm())
    {
        @*@Html.DropDownList("ID_P", new SelectList(ViewBag.Pages, "ID_P", "Title", ViewBag.Page))
        <button type="submit">Tìm</button>*@
        <br />
        <table id="Bang" style="background-color: #e2f5f3">

            <tr><th>ID</th><th>TÊN</th><th>TÊN FILE</th><th>XEM</th></tr>
            @foreach (PageItem m in ((ViewBag.Pages) as IEnumerable<PageItem>))
            {
                <tr>
                    <td>@m.ID_P</td>
                    <td>@m.Title</td>
                    <td>@m.Name_File</td>
                    <td>@Html.ActionLink("Xem", "ViewTaiLieu_Page", new { id = m.ID_P }, new { @class = "link" })</td>

                </tr>
            }
        </table>
        
    @*<div id="phanTrang">
        Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
              @Html.PagedListPager(Model, page => Url.Action("DSTaiLieu", new { _id = page}))
    </div>*@
    }


    <i class="fas fa-caret-square-left"></i> @Html.ActionLink("Trở về ", "TaiLieu", "Home")

</div>
